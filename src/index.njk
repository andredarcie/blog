---
layout: base.njk
title: Início
lang: pt
translationUrl: /en/
---

<section class="home">
  <h1>Bem-vindo ao blog</h1>
  <p class="lead">Escrevo sobre tecnologia, sobre como penso e sobre o que me impressiona.</p>

  <ul class="post-list">
    {% for post in collections.postsPt %}
    <li>
      <a href="{{ post.url | url }}">{{ post.data.title }}</a>
      <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("pt") }}</time>
    </li>
    {% endfor %}
  </ul>
</section>
